# Server Frontend (Placeholder)

This Express server exposes a `/health` route and is intended to evolve into an API to:
- Upload/parse Excel
- Trigger automation runs
- Stream logs and results

Do not auto-start in CI. To run locally:
- npm run dev:server
